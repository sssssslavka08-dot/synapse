import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // ── Brand ──
  static const tiffany = Color(0xFF0ABDB9);
  static const tiffanyLight = Color(0xFFD6F5F4);
  static const tiffanyMid = Color(0xFF3FCFCC);
  static const tiffanyDark = Color(0xFF078987);
  static const tiffanyGlow = Color(0x260ABDB9);

  // ── Light theme (legacy) ──
  static const mint = Color(0xFFE8FAFA);
  static const bg = Color(0xFFF4FEFE);
  static const white = Color(0xFFFFFFFF);

  // ── Dark theme palette ──
  static const darkBg = Color(0xFF131F24);
  static const darkSurface = Color(0xFF1B2B31);
  static const darkCard = Color(0xFF1F333A);
  static const darkCardHover = Color(0xFF253D45);
  static const darkBorder = Color(0xFF2A4149);
  static const darkNav = Color(0xFF162329);

  // ── Text ──
  static const dark = Color(0xFF0F1F1E);
  static const dark2 = Color(0xFF1E3332);
  static const textPrimary = Color(0xFFECF4F3);
  static const textSecondary = Color(0xFF8EAEAC);
  static const textHint = Color(0xFF5B7D7A);
  static const textMuted = Color(0xFF3E5A57);

  /// Text on light surfaces (white cards, kids cream bg)
  static const onLight = Color(0xFF0F1F1E);
  static const onLightSecondary = Color(0xFF4D6766);
  static const onLightMuted = Color(0xFF6B8A88);

  // ── Accent ──
  static const xpGold = Color(0xFFFFBB33);
  static const streakOrange = Color(0xFFFF6B35);
  static const successGreen = Color(0xFF22C55E);
  static const errorRed = Color(0xFFEF4444);
  static const heartPink = Color(0xFFFF4B7A);
  static const gemPurple = Color(0xFF8B5CF6);
}
