services:
  - type: web
    name: synapse-api
    env: static
    buildCommand: echo "Static site"
    staticPublishPath: admin
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
